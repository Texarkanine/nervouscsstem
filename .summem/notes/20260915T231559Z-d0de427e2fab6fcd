Catalog examples: island inner HTML is the copyable html fence; JS scripts querySelector a class from that markup (parentElement when init looks up descendants). Scanlines stays fence-only (viewport overlay).
