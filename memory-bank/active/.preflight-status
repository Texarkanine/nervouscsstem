PASS WITH ADVISORY

Level 3 plan for sine-wave-graph (issue #8), validated against the worktree on 2026-09-23. No plan edits were made (no TDD step swap, no strike).

Prerequisites pass. Planning is complete in `tasks.md`. Both creative docs exist (`creative-wave-rendering.md`, `creative-wave-api.md`) and match the plan's decisions.

TDD plan encoding passes. The executable units are step 1 (token) and step 2 (the partial). In both, tests are stubbed (1.1) and written red (1.3 / 2.3) before production code (1.4 / 2.4). The tests assert on compiled `dist/nerv.css`, which is the shipped product, following the repo's regex and block-slicing convention. B1–B3 evaluate the shipped Bézier geometry numerically, so they are behavior tests, not change-detectors. Steps 4 and 5 (docs page, ref fixture) are prose and visual fixtures and correctly have no tests. Step 3 is a verification harness outside the repo.

Convention compliance passes, with advisories below. Checked items: `.nerv-` prefix; the `@forward` after `radar`, plus the header order comment; a `:root` duration token like `--nerv-radar-duration`; `$nerv-colors` glow-flagged modifier generation, as in reticle and grid-marks; the `nerv-glow-drop` mixin (its signature takes the property name, so pass `'--nerv-wave-color'`); data URIs quoted as in `_grid-marks.scss`, which satisfies stylelint `function-url-quotes`; and the docs island plus verbatim fence convention.

Dependency impact passes. No existing test enumerates `:root` tokens, `@forward` modules, or keyframes exhaustively, so none will break. The existing chrome composes cleanly on the graph box. `.nerv-grid-marks` is a `background-image`, and `.nerv-reticle-*` uses `position: relative` plus `::after` with `z-index: 0`, which paints above the absolutely positioned waves in tree order. The plan's "graph sets no background, border, or pseudo-elements" invariant (B15) is exactly what keeps both working. `docs-init.js` routes JavaScript islands only, and the wave has no hook, so it needs no change. `docs/components/css/heavies/index.md` has no page list (awesome-pages infers it), so it needs no edit.

Conflict detection passes. There are no existing `nerv-wave*` selectors and no `@property` rules anywhere in `src/`, so `--nerv-wave-t` is the first registered property. The color modifier names (amber, …, steel) do not collide with `-graph`, `-point`, or `-reverse`. The change is additive and breaks no public contract.

Completeness passes. Requirements 1–10 and acceptance criteria 1–7 each map to concrete steps. Scroll speed = f × λ holds because the mask offset per period is exactly one wavelength. The cascade is covered statically by B8 and live by the step 3 harness under `nerv-state-critical`.

## Advisories

1. **Reference still (step 4.1), should fix during build.** The plan says "copy the reference still to `docs/img/wave-graph.jpg`" but gives no source, and no local copy exists outside the repo. The issue's still is already in the repo's LFS library: `docs/img/3RBI9q8.png` (NGE-1 00:19:59, the ±5 axis, the timecode, the interleaved red/blue sinusoids; cataloged in `docs/visual-language/design-language.md` §7). Every file in `docs/img/` is PNG, and heavies pages use a named 704×480 PNG copy (`radar.png`, `reticles.png`). Use `docs/img/wave-graph.png`, copied from `3RBI9q8.png`, not a `.jpg` downloaded from the GitHub attachment.
2. **TDD sequencing clarity, low.** Step 1.3 writes all B1–B18 tests, which makes step 2.3 a duplicate, and step 2.2 (interface stub) then comes after the tests are written. Tests still come first, so this is not a violation. The simplest reading: write every test in 1.3, run red, then do 1.4, 2.2, and 2.4, with 2.3 as a re-run.
3. **Test file placement, low.** Heavies are currently tested in the layer file `test/patterns.test.mjs` (radar is at `describe('Radar CSS')`). A dedicated `test/wave-graph.test.mjs` is a reasonable choice, given the Bézier parser helper, but it departs from the per-layer grouping. Keep it, but register it in `package.json` as planned.
4. **"Timing why" home, low.** `_radar.scss` points its timing rationale to `docs/visual-language/radar.md`. The plan puts the wave's rationale only in the SCSS header comment. That is fine for this scope. Optionally, the catalog page could link `design-language.md` §7 and the Sync Curve Trace entry in `atomic-elements.md` as the visual lineage.

## Radical Innovation Advisory

**Desync through the cascade, the show's actual story.** Add one ambiance token, `--nerv-wave-desync` (default `0`), that `_states.scss` raises under the alert states, plus one per-wave `--nerv-wave-desync-index` (default `0`, set automatically by `:nth-child` in the same way as `--nerv-stagger-index`). Add a single term, `desync × desync-index`, to the phase inside the one-clock formula, in both the `::before` mask-position and the point `sin()`. Because both read the same sum, stroke and points still cannot drift apart, and the pinned invariant survives. The effect: the four-phase sync graph sits locked at `nerv-state-normal` and visibly fans apart under warning and critical, which is the "sync ratio collapsing" read from the dual-helix waveform stills, and it costs zero JavaScript and no new animation. Cost: two custom properties, one state override per alert class, and two tests (the term appears in both the stroke and point expressions; states set the token). The plan's API and rendering stay intact, so this can be added now or as a follow-up issue.
