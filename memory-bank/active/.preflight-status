PASS WITH ADVISORY

Preflight run 2026-09-13 for task pr10-review-fixes (Level 2). All seven default-preflight checks ran; no blocking or fixable findings. No plan edits (no TDD step swap, no change-detector strike) were needed.

Check results:

1. Wrong-reference guard: progress.md records Level 2; default reference is correct.
2. Prerequisites: tasks.md planning sections complete (test plan, implementation plan, technology validation, pre-mortem all checked). L2 requires no creative docs; none exist.
3. TDD Plan Encoding (blocking check): PASS. Unit 1 (docs-assets tests) orders stub-tests -> write-tests-red -> code-green. Unit 2 (PyPI relock) reuses unit 1's lock assertion before the relock step. The uv.lock assertion is a real contract test (goes red when the lock is re-generated against the CUDA channel, breaking frozen docs CI), not a change-detector. Units 3 (docs:build script string) and 4 (bar-meter markdown) are prose/policy artifacts and correctly owe no tests; the plan explicitly declines change-detectors on them.
4. Convention Compliance: PASS. Tests land in existing test/docs-assets.test.mjs per the test/<area>.test.mjs convention; package.json's explicit test-file list already includes it. All other touchpoints (pyproject.toml, uv.lock, package.json, docs/components/bar-meters.md, skills/nerv/docs/components/bar-meters.md) exist as planned.
5. Dependency Impact: PASS. The docs:build prefix mirrors the existing docs:serve pattern (npm run build && resolve-docs-assets --mode local && ...). The skill-copy byte-identity obligation is backed by the existing test/skill-contract.test.mjs; verified the two bar-meters.md copies are currently identical, so a single edit must be mirrored. Relock verification (uv sync --group docs --frozen, local docs build) is planned.
6. Conflict Detection: PASS. Production already dispatches nerv-docs:ready (scripts/resolve-docs-assets.mjs line 70) and docs-init.js already listens for it (line 28); the plan correctly notes the handshake test may be green immediately and forbids weakening production to force red. The contamination source is confirmed real: ~/.config/uv/uv.toml defines the pytorch-cu126 extra index, and uv.lock currently contains 45 download.pytorch.org lines, so the lock assertion will be red as expected. pyproject.toml has no index config today, so pinning the default index there is additive, not a conflict.
7. Completeness Precheck: PASS. All four projectbrief requirements map to plan units 1-4 with concrete files and approaches; acceptance criteria are covered.

Advisory findings (non-blocking):

- Radical-innovation advisory: strengthen the lock test from a blacklist to a whitelist. The plan asserts uv.lock does NOT contain download.pytorch.org, which catches exactly one known channel. The stronger, still-cheap contract is: every registry URL in uv.lock resolves under files.pythonhosted.org / pypi.org. That fails on any future non-PyPI index (a different CUDA channel, a corporate mirror, a git URL dependency), not just the one we got bitten by. Structural sketch: parse `url = "..."` lines from uv.lock in the same test file and assert each matches the PyPI host allowlist. Cost: a few lines in test/docs-assets.test.mjs; no plan restructure. Operator to evaluate at build time.
- Verify the exact uv setting key during build: the plan says "pin [tool.uv] default index". uv accepts both default-index in [tool.uv] and the [[tool.uv.index]] table with default = true; confirm against the installed uv version's docs/schema and prefer the non-deprecated form. The isolated relock invocation (uv lock --default-index ... / --config-file) is correct regardless.
- The handshake assertion will pass on first run (production already dispatches). That is expected and fine; the test's value is as a regression guard. No action needed.
