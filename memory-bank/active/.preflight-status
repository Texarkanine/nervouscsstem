PASS WITH ADVISORY

Preflight run 2026-09-12, task nerv-v01-m4-properdocs-dual-load-site (Level 3), against the replanned tasks.md (embedded-islands plan with job-scoped pages permission).

Check results:

1. Wrong-reference guard: PASS. progress.md Complexity is Level 3; default-preflight is the correct reference.
2. Prerequisites: PASS. tasks.md Status shows planning through Pre-Mortem complete; creative docs exist (creative-embedded-docs-examples.md active, creative-dual-load-example-hosting.md marked superseded).
3. TDD Plan Encoding: PASS. Unit 1 (resolve-docs-assets.mjs) is the only executable unit — the local/CDN load-path contract is observable by docs-site visitors and by the operator running a local build. Its numbered steps run stub-tests -> stub-interface -> write-tests-run-red -> write-code-run-green; a reasonable implementer cannot follow it code-first. Units 2-4 are prose/policy (SSG config, markdown teaching pages, workflow YAML, README/techContext pointers) and owe no tests. The prior preflight's strike of document-content change-detectors is honored; no change-detector steps remain.
4. Convention Compliance: PASS. test/docs-assets.test.mjs matches the test/*.mjs convention with explicit paths in package.json "test". The data-nerv-init opt-in attribute mirrors the existing data-nerv-hex-flicker pattern in src/nerv.js. New scripts/ directory has no prior convention to violate; .mjs matches the repo's ESM test style.
5. Dependency Impact: PASS. Verified against the codebase: the publish-npm job exists in release-please.yaml, so unit 3's needs: publish-npm resolves; the workflow-level permissions map (contents/id-token/issues/pull-requests) omits pages: write, and the plan's job-scoped pages: write + id-token: write + contents: read is the correct minimal grant. extra_javascript ordering [nerv.js, docs-init.js] guarantees window.NERV exists before docs-init runs. test/publish-contract.test.mjs is unaffected because package.json files stays dist-only.
6. Conflict Detection: PASS. No existing scripts/, properdocs.yml, or pyproject.toml to collide with. Public package contract (files, main, style) unchanged. jsDelivr URL package name matches package.json "nervouscsstem"; site_url matches the Texarkanine/nervouscsstem repo. Verified in src/nerv.js: initBarMeters(container) accepts an element scope (line 155), injectScanlines appends a fixed overlay to document.body (line 93-100) — confirming both the plan's scoped-init approach and the hard rule that docs-init.js must never call NERV.init(). .nerv-panel variants and the data-bars / data-fill bar-meter contract exist in src/, matching the planned Using pages. The docs/service-manual.md ../.gitattributes link slated for a blob-URL fix exists at line 23.
7. Completeness Precheck: PASS. Requirements 8-12 and AC 3-5 each map to concrete steps: site config and Using pages (unit 2), dual-load resolver with throw-on-missing-dist (unit 1), Pages deploy gated on npm publish (unit 3), README/techContext pointers (unit 4 — README's ## Documentation section exists at line 26 as the anchor). Test coverage is planned for all new executable behavior and only that behavior.

Advisory findings (non-blocking):

- RADICAL INNOVATION: Close the jsDelivr-lag hole with a propagation gate. Challenges already names the risk that jsDelivr has not picked up a just-published version when the Pages job runs; needs: publish-npm only orders the jobs, it does not make the CDN URL serve. The single highest-leverage addition to the plan: in CDN mode, after computing the versioned URL, HEAD-request it with bounded retry/backoff and throw if it never 200s — turning a silently-broken live site into a failed (retryable) release job. Structural sketch: resolveDocsAssets({ mode, root, verify }) where verify is an injectable fetch used only by the release workflow's --verify flag; unit tests assert the retry/throw against a stubbed fetch, so node:test stays hermetic. Cost: one flag, one injected dependency, two or three test cases. Left for the operator to weigh against redesign cost; the plan is buildable without it.
- MINOR: .nerv-docs-island spends the design system's .nerv- namespace on docs-site-only chrome. It is the safe choice (avoids Material collisions; stylelint only lints dist/nerv.css), but be aware the prefix no longer exclusively means "shipped design system." No action needed.
- MINOR: docs:serve chains npm run build on every invocation, so docs-only edits pay a sass rebuild. Trivial at this repo's scale; revisit only if docs iteration becomes the common case.
