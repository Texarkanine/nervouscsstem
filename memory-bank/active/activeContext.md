# Active Context

## Current Task: Canonical usage guide
**Phase:** POST-REFLECT - nested catalog implemented

## What Was Done
- Nested CSS as `core` / `structure` / `atoms` / `heavies`. JS mirrors leaves that have a hook.
- Folded dividers into panels. Folded gradients into colors.
- Polar radar (cqmin / phosphor origin) was already saved; catalog links now point at `css/heavies/radar.md` and `javascript/heavies/radar.md`.
- Cut usage out of `visual-language/radar.md` into the CSS/JS radar catalog pages. VL keeps the timing why.

## Next Step
- Confirm `docs:build --strict` and `npm test`. `/niko-archive` after the operator is satisfied.
