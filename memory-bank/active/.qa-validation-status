PASS

QA semantic review of nerv-v01-m4-properdocs-dual-load-site against the replanned tasks.md and creative-embedded-docs-examples.md.

Verified against plan:
- Resolver (scripts/resolve-docs-assets.mjs): local copy-or-throw into gitignored docs/ paths; CDN jsDelivr stand-ins at the same paths; version from package.json (no @latest, no hardcoded version); CLI --mode flag; no ref//src/ writes.
- Tests (test/docs-assets.test.mjs): 7 cases covering all 6 test-plan behaviors; registered explicitly in package.json "test" per repo convention.
- properdocs.yml: docs_dir, material theme, strict, site_url, repo_url/edit_uri, md_in_html, mermaid superfences, extra_css/extra_javascript per creative, full nav.
- Island chrome: docs-init.js never calls NERV.init/injectScanlines; scoped initBarMeters on [data-nerv-init] only; window.NERV-first then nerv-docs:ready listener handles deferred extra_javascript in both modes. Confirmed src/nerv.js never self-initializes, so loading nerv.js site-wide is scanline-safe.
- Using pages: css.md and panels.md CSS-only (no data-nerv-init); bar-meters.md opts in with data-nerv-init="bar-meters"; fenced sample shows initBarMeters, not NERV.init(). Each example is island, spec, code.
- docs/index.md present; service-manual.md ../.gitattributes link replaced with GitHub blob URL.
- Workflows: PR docs build local mode with lfs/Node 24/uv --frozen; publish-pages needs publish-npm, job-scoped pages: write + id-token: write + contents: read, CDN mode, strict build, upload-pages-artifact + deploy-pages, github-pages environment. Workflow-level permissions map untouched.
- README Pages URL + Using-pages note; techContext.md surgical uv/ProperDocs + dual-load pointer.
- No skill, extra-files, or vendored fonts added (scope held).

Advisories (non-blocking):
- jsDelivr-lag 404 leaves islands silently uninitialized; accepted risk (needs: publish-npm; preflight HEAD-verify advisory explicitly deferred).
- .nerv-docs-island spends the .nerv- product namespace on docs-only chrome; preflight advisory, accepted.
- Mixed .yaml/.yml workflow extensions match the plan's filenames; cosmetic only.
