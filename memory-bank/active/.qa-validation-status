FAIL

QA of sine-wave-graph (issue #8), implementation commit `badf035`, reviewed against the Level 3 plan in `tasks.md` and both creative docs on 2026-09-23. Result: Build must rerun for two documentation fixes. The code itself is acceptable as-is.

Mechanical checks, rerun by QA: `npm test` 392/392 pass. `npm run docs:build` (strict) passes. `npm run lint` shows 10 errors, all in radar, cartouche, and typography output that predates this task; none are in wave-graph rules.

Code review against the plan:

- Completeness: every planned behavior (B1–B18) is implemented and tested. There are no TODOs, stubs, or placeholder strings. The docs page, the ref fixture, the token, the forward, and the Heavies blurb are all present.
- Correctness, checked by hand: the stroke mask offset and the point `sin()` share one phase expression (`s/λ + φ + t`) in both orientations. The signs match (SVG y points down, so the path uses -sin and the point uses `50% - …`; the vertical path uses +sin and the point uses `50% + …`). The tile cross size (A × 200cq) against the ±2 viewBox gives one amplitude unit = A × 50%, which equals the point offset. Specificity holds: the vertical and contrast overrides win where they should, `-reverse` follows the shorthand, and the color modifiers follow the box defaults.
- KISS, DRY, YAGNI: no violations. Using one Sass path function for both orientations and one shared `$_phase-turns` string is proportionate. `pointer-events: none` and the point margin/padding reset are small defensive choices, not speculative features.
- Regression: naming, `@use tokens` / `glow`, the `$nerv-colors` modifier loop, and the contrast/reduced-motion blocks follow the existing module patterns (reticle, grid-marks, radar).
- Integrity: the magic numbers are Bézier control constants, stroke widths, and a 6px floor. Each has a comment or a creative-doc rationale. There are no debug artifacts.

Blocking findings (Build rerun):

1. Documentation: `docs/components/css/core/colors.md` ("Glow colors") lists every family that generates `{name}` color modifiers ("stripes, tables, forms, cartouches, reticles, grid marks, and gradient from/to"). That list matches every other `@each $name` loop in `src/`, but it does not name the new `.nerv-wave-{name}`. The list is presented as complete, so it is now stale. Fix: add wave graphs to it.
2. Documentation: the wave-graph page says "Older browsers show the traces holding still." That is true for engines that lack animated `@property` or `sin()`. It is false for Chromium 85–119. Those versions support only `-webkit-mask-*`, so the `::before` stroke ignores its unprefixed `mask-image` and paints a solid `--nerv-wave-color` rectangle over the whole box. Fix: either correct the sentence (for example, "below the support floor the graph is not supported; Chromium before 120 paints a solid block"), or make the fallback true (for example, gate the stroke on `@supports (mask-image: url(''))`). Build picks one. Fixing only the wording is enough.

Advisories (non-blocking):

- The `.summem` nap compaction (two notes folded into a nap) and one new summem note landed in the `feat:` commit. They are tool-managed and harmless, but they mix unrelated churn into the feature commit.
- The `src/nerv.scss` header says downstream modules consume tokens "not @use". Many existing modules already `@use 'tokens'`, so this comment was inaccurate before this task. Out of scope here.
- `memory-bank/systemPatterns.md` (registered-clock pattern, filter-before-mask gotcha) is deferred to Reflect, as planned.
