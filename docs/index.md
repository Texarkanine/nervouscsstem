# NERV design system

Pure CSS (optional `nerv.js`) for NERV operational consoles.

Released pages load CSS and JS from the npm CDN. A local serve or build uses `dist/` and errors if those files are missing.
